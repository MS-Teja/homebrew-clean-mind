# homebrew-clean-mind

Homebrew tap for [Clean Mind](https://github.com/MS-Teja/clean-mind), a disk
usage analyzer that understands developer bloat.

```sh
brew install --cask MS-Teja/clean-mind/clean-mind
```

Installing via Homebrew also removes the Gatekeeper quarantine attribute, so
the app opens normally on first launch.
